services:
  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./.mongo:/data/db
    restart: always
  app:
    build: .
    ports:
      - ${PORT}:${PORT}
    depends_on:
      - mongo